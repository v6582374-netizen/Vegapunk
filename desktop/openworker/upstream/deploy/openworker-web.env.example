# Required for a network-facing Linux Web Counterpart. Generate with:
#   python -c 'import secrets; print(secrets.token_urlsafe(32))'
COWORKER_WEB_TOKEN=replace-me
COWORKER_STATE_DIR=/var/lib/openworker
