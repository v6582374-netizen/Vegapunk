第 {RUN_NUM} 次运行已完成。结果如下：
{RESULTS}

请根据这些结果：
1. 检查当前分数（满分 100 的 total_score）以及哪些检查清单项最弱。
2. 分析你的实现中哪些有效、哪些无效。
3. 决定要改进什么：修复错误、提高准确性、补充缺失的检查清单项，或完善报告。
4. 在 `code/` 中实施改进，并更新 `report/report.md` 和 `report/images/`。

第 {NEXT_RUN_NUM} 次运行将通过 `bash launcher.sh` 执行。
不得修改 `launcher.sh`。

如果你认为复现已完成且达到最优，请回复 'ALL_COMPLETED'。

聚焦权重最高的检查清单标准——它们对最终分数贡献最大。
确保 `report/report.md` 存在，且使用实际数值和图表引用描述**所有**结果。
