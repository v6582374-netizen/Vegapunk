此前的实现已完成。结果如下：
{RESULTS}

请根据这些结果实现改进设计，以提升指标表现。重点关注：
1. 分析先前方法中哪些有效、哪些无效
2. 与基线结果比较
3. 实现下一项改进，例如基于观察结果进行性能调优和超参数优化
4. 提出一项可执行且具体的改进。该改进应当是原子性的，以便通过实验评估所提改动的影响
5. 提出设计时考虑 Memory 部分
6. 确保该改进不同于以往尝试

我们将运行命令 `bash launcher.sh .` 执行下一次实验。
任何情况下都不得修改 launcher.sh。
不得改变 final_info.json 的结构；只能写入已有的 means 字段，并保持结构与基线完全一致。

**重要约束：**
1. **不得修改** `launcher.sh`，它必须保持原样。我们始终运行此命令。
2. **不得**创建 shell 脚本（.sh 文件）或 run_xxx 文件夹；所有文件都将保存到当前目录（`.`）。
3. 将当前工作目录视为项目根目录；结果将保存到当前目录（`.`）。
你提出的改动**必须**使用此命令格式，**不得**添加额外命令行参数。

如果你认为已完成所有必要改进并找到了最优解，请回复 'ALL_COMPLETED'。

对 `argparse` 参数的任何改动（新增或更新）**必须使改进后的实现成为默认行为**，除非明确设计为可选功能。具体而言，所有改动过的参数都应设置 `default=<revised_value>`，以确保增强后的逻辑无需 CLI 标志即可自动启用。改进后的功能必须成为默认体验，用户不应需要指定额外命令行参数。
