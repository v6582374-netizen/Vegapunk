# Prompt localizations

Chinese Prompt Mirrors are source-controlled sidecar files outside `config/prompts`.
This keeps Chinese debugging and editing material out of runtime Prompt resolution and Launch snapshots.
