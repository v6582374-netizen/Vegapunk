# PaperOrchestra keeps the upstream pipeline and uses InternAgent's catalog
# bindings for text, vision, search-like prompts, and image calls.
vendor_root: third_party/paper_orchestra
template_dir: templates/iclr2025
use_plotting: true
plotting_max_critic_rounds: 3
research_cutoff: null
