# Use a Standard DashScope API Key for Qwen

The initial Qwen Model Provider and Qwen Code Backend will authenticate with a standard DashScope API key through the OpenAI-compatible DashScope endpoint.
Alibaba Cloud Coding Plan and the retired Qwen OAuth flow are outside the initial integration so that model selection and usage remain directly configurable through the existing project configuration.
